name = "promptfolio"
compatibility_date = "2024-04-05"

[vars]
SITE_URL = "${SITE_URL}"
GEMINI_MODEL = "${GEMINI_MODEL}"

[[d1_databases]]
binding = "DB"
database_name = "${DB_NAME}"
database_id = "${DB_ID}"
